const Router={current:"dashboard",titles:{dashboard:"Guten Morgen, Mama 👋",intake:"Einnahmen heute",cabinet:"Medikamentenschrank",calendar:"Kalender",more:"Mehr",emergency:"Notfall"},init(){document.querySelectorAll("[data-view]").forEach(b=>b.onclick=()=>this.show(b.dataset.view));document.querySelectorAll(".back-btn").forEach(b=>b.onclick=()=>this.show("dashboard"));document.querySelectorAll("[data-open=emergency]").forEach(b=>b.onclick=()=>this.show("emergency"));appointmentCard.onclick=()=>this.show("calendar");emergencyCard.onclick=()=>this.show("emergency");showLog.onclick=()=>this.show("intake")},show(name){this.current=name;document.querySelectorAll(".view").forEach(v=>v.classList.remove("active-view"));document.getElementById("view-"+name).classList.add("active-view");document.querySelectorAll(".nav-btn").forEach(b=>b.classList.toggle("active",b.dataset.view===name));pageTitle.textContent=this.titles[name]||"MediPlan";this.refreshCurrent();window.scrollTo({top:0,behavior:"smooth"})},refreshCurrent(){if(this.current==="intake")Dashboard.renderIntakePage();if(this.current==="cabinet")Dashboard.renderCabinet();if(this.current==="dashboard")Dashboard.render()}};