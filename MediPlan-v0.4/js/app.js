const UI={timer:null,toast(msg){toast.textContent=msg;toast.classList.add("show");clearTimeout(this.timer);this.timer=setTimeout(()=>toast.classList.remove("show"),2400)}};
document.addEventListener("DOMContentLoaded",()=>{Settings.init();Intake.load();Dashboard.init();showLog.onclick=()=>{logSection.classList.remove("hidden");logSection.scrollIntoView({behavior:"smooth"})};hideLog.onclick=()=>logSection.classList.add("hidden");navIntake.onclick=showLog.onclick;resetBtn.onclick=()=>{if(confirm("Heutige Testdaten zurücksetzen?"))Intake.reset()};if("serviceWorker"in navigator)navigator.serviceWorker.register("./service-worker.js").catch(console.error)});